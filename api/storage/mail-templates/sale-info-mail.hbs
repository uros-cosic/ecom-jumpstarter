<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{heading}}</title>
        <style>
            body {
              margin: 0;
              padding: 0;
              font-family: Arial, sans-serif;
              background-color: #f4f4f4;
              color: #333;
            }
            table {
              border-spacing: 0;
              width: 100%;
            }
            td {
              padding: 0;
            }
            img {
              border: 0;
              display: block;
            }
            .container {
              max-width: 600px;
              margin: 0 auto;
              background-color: #ffffff;
              overflow: hidden;
              border: 1px solid #dddddd;
            }
            .header {
              background-color: {{primaryColor}};
              color: {{primaryForegroundColor}};
              padding: 20px;
              text-align: center;
            }
            .header h1 {
              margin: 0;
              font-size: 24px;
            }
            .content {
              padding: 20px;
            }
            .content h2 {
              margin: 0 0 10px;
              font-size: 20px;
            }
            .content p {
              margin: 0 0 20px;
              font-size: 16px;
              line-height: 1.5;
            }
            .sale-banner {
              margin: 20px 0;
              text-align: center;
            }
            .sale-banner img {
              max-width: 100%;
              height: auto;
            }
            .cta-btn {
              display: inline-block;
              padding: 10px 20px;
              background-color: {{primaryColor}};
              color: {{primaryForegroundColor}};
              font-size: 18px;
              font-weight: bold;
              text-decoration: none;
              border-radius: 4px;
              text-align: center;
            }
            .footer {
              background-color: #f4f4f4;
              text-align: center;
              padding: 20px;
              font-size: 14px;
              color: #666;
            }
            .footer a {
              color: {{primaryColor}};
              text-decoration: none;
            }
            @media (max-width: 600px) {
              .content h2 {
                font-size: 18px;
              }
              .content p {
                font-size: 14px;
              }
            }
        </style>
    </head>
    <body>
        <table class="container">
            <tr>
                <td>
                    <div class="header">
                        <h1>{{heading}}</h1>
                    </div>
                    <div class="content">
                        <h2>{{subheading}}</h2>
                        <p>{{saleIntro}}</p>
                        <div class="sale-banner">
                            <img src="{{saleImage}}" alt="{{saleImageAlt}}" />
                        </div>
                        <p>{{ctaMessage}}</p>
                        <a href="{{saleLink}}" class="cta-btn">
                            {{ctaButtonText}}
                        </a>
                    </div>
                    <div class="footer">
                        <p>
                            {{supportMessage}}
                            <a href="{{contactLink}}">{{contactUsHere}}</a>.
                        </p>
                        <p>
                            &copy; {{year}} {{companyName}}. {{allRightsReserved}}
                        </p>
                    </div>
                </td>
            </tr>
        </table>
    </body>
</html>

